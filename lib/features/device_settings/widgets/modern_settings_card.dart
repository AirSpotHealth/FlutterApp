import 'package:airspothealth/features/device_settings/widgets/settings_card.dart';

// Alias kept for backward compatibility — use SettingsCard directly for new code.
typedef ModernSettingsCard = SettingsCard;
