# Slim 0.14.6 iOS compatibility

Implement local signed-file Slim OTA, correct demonstrated BLE parser mismatches, preserve Screen behavior and existing user edits, and verify with focused automated tests plus physical iPhone qualification. Keep hosted Slim updates disabled.

Implementation order: protocol compatibility → image validation → exclusive OTA lifecycle and verified reconnect → automated checks → physical qualification.

The decision record, commands, acceptance checklist and evidence are maintained in [the qualification report](../docs/slim-0.14.6-ios-qualification.md).
